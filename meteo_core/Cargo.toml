[package]
name = "meteo_core"
version = "0.1.0"
edition = "2024"
publish = false

# Логика прошивки без железа: формат пакета, LED-палитра, решение watchdog,
# выбор сети, автомат отправителя. `no_std`, без esp-зависимостей ⇒ тесты
# идут на хосте (`cargo test` в этой директории): `meteo/.cargo/config.toml`
# с riscv-таргетом сюда не дотягивается.

[dependencies]
aes-gcm = { version = "0.11.1", default-features = false, features = ["aes"] }
# serde: декодирование батча приёмником в тот же тип, что кодирует прошивка.
heapless = { version = "0.9", features = ["serde"] }
# experimental-derive: `#[derive(MaxSize)]` — бюджет пакета проверяется при
# компиляции (wire.rs).
postcard = { version = "1.1.3", default-features = false, features = ["experimental-derive"] }
serde = { version = "1.0.229", default-features = false, features = ["derive"] }

[dev-dependencies]
proptest = "1.11"
